# Performance Comment Routing

Classify requests about strength, bonding, durability, viscosity, or rheology
as `comment_type=performance`.

For a risk audit, keep measured results separate from requested tests. Load
`references/experiment-revision.md` only when `response_task=revision-plan`;
use remediation plans when an experiment must be scoped.
