interface:
  display_name: "Civil Materials Shared"
  short_description: "Shared references for civil materials skills."
  default_prompt: "Use the civil-materials task-specific skills instead of invoking shared references directly."
  brand_color: "#3F6F5E"

policy:
  allow_implicit_invocation: false
